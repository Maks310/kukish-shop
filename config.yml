backend:
  name: github
  repo: ваш-логін/kukish-shop
  branch: main

media_folder: "images"
public_folder: "/images"

collections:
  - name: "products"
    label: "Товари"
    folder: "products"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Назва", name: "title", widget: "string" }
      - { label: "Ціна", name: "price", widget: "string" }
      - { label: "Опис", name: "body", widget: "markdown" }
